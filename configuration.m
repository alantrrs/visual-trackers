
set_global_variable('version', 4);

% Enable more verbose output
% set_global_variable('debug', 1);

% Disable result caching
% set_global_variable('cache', 0);

% Disable result packaging
% set_global_variable('pack', 0);

% Set empirical data path
set_global_variable('data_path', '/data')

% Set empirical workspace path
set_global_variable('directory', '/workspace')

% Select experiment stack
set_global_variable('stack', 'vot2016');
